pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build Successfully'
            }
        }
        stage('test') {
            steps {
                echo'Tested successfully'
            }
        }
    }
}
